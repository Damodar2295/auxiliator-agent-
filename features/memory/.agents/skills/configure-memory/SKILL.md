---
name: configure-memory
description: Configure LangGraph memory or PostgreSQL checkpoint persistence.
---
# Configure Memory

Set `LANGGRAPH_CHECKPOINTER_BACKEND` to `memory` or `postgres`. PostgreSQL also requires `LANGGRAPH_CHECKPOINTER_DB_URI` and `langgraph-checkpoint-postgres`.
