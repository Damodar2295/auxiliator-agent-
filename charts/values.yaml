replicaCount: 1
image:
  repository: REPLACE_WITH_REGISTRY/auxiliator-agent
  tag: latest
  pullPolicy: IfNotPresent
service:
  port: 8080
resources: {}
env:
  EPAS_ENV: e1
  LANGGRAPH_CHECKPOINTER_BACKEND: memory
