---
name: configure-rag
description: Configure the Auxiliator RAG document store, embedding model, and retrieval parameters.
---
# Configure RAG

Collect the document-store and embedding variables listed in `.env.example`. Validate that `EMBEDDING_DIMENSION` matches the selected model, then verify connectivity and retrieval before finishing.
